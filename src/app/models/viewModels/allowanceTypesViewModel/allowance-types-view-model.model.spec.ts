import { AllowanceTypesViewModel } from './allowance-types-view-model.model';

describe('AllowanceTypesViewModel', () => {
  it('should create an instance', () => {
    expect(new AllowanceTypesViewModel()).toBeTruthy();
  });
});
