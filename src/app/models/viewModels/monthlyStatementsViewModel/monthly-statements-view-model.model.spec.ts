import { MonthlyStatementsViewModel } from './monthly-statements-view-model.model';

describe('MonthlyStatementsViewModel', () => {
  it('should create an instance', () => {
    expect(new MonthlyStatementsViewModel()).toBeTruthy();
  });
});
